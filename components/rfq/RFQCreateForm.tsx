"use client";
import { useState } from "react";
import { PlatformShell } from "@/components/platform/PlatformUI";
import { Card } from "@/components/ui/card";
export function RFQCreateForm() { const [sent, setSent] = useState(false); if (sent) return <PlatformShell eyebrow="Procurement request" title="Your RFQ has been submitted" description="Your request is now prepared for supplier responses. This static demo has not contacted a real supplier."><Card className="mt-8 max-w-2xl"><p className="text-slate-600">We saved your procurement request locally and will connect it to supplier responses when the backend is implemented.</p><button onClick={() => setSent(false)} className="mt-5 rounded-full border border-slate-300 px-4 py-2 text-sm font-semibold">Create another RFQ</button></Card></PlatformShell>; return <PlatformShell eyebrow="Procurement request" title="Post an RFQ" description="Tell qualified suppliers what you need and compare responses in one place."><Card className="mt-8"><form onSubmit={(e) => { e.preventDefault(); setSent(true); }} className="grid gap-4 sm:grid-cols-2">{["Product or service required", "Category", "Quantity", "Unit", "Target price", "Currency", "Destination country", "Delivery location", "Required delivery date", "Supplier requirements"].map((label) => <label key={label} className="text-sm text-slate-600">{label}<input required={label !== "Target price" && label !== "Supplier requirements"} type={label.includes("date") ? "date" : "text"} className="mt-1 w-full rounded-lg border border-slate-200 px-3 py-2" /></label>)}<label className="text-sm text-slate-600 sm:col-span-2">Detailed requirements<textarea required className="mt-1 min-h-28 w-full rounded-lg border border-slate-200 px-3 py-2" /></label><label className="text-sm text-slate-600 sm:col-span-2">Additional message<textarea className="mt-1 min-h-20 w-full rounded-lg border border-slate-200 px-3 py-2" /></label><label className="rounded-xl border-2 border-dashed border-slate-300 p-5 text-center text-sm text-slate-500 sm:col-span-2">Attach specifications or reference files<input type="file" multiple className="mt-3 block w-full text-xs" /></label><button className="rounded-full bg-indigo-600 px-5 py-3 text-sm font-semibold text-white sm:col-span-2">Submit RFQ</button></form></Card></PlatformShell>; }
