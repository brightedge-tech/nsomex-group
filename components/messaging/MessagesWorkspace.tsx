"use client";
import { useState } from "react";
import { messages } from "@/lib/data/platform";
import { Card } from "@/components/ui/card";
import { PlatformShell } from "@/components/platform/PlatformUI";
export function MessagesWorkspace() { const [selected, setSelected] = useState(messages[0]); const [query, setQuery] = useState(""); const filtered = messages.filter((m) => `${m.name} ${m.product}`.toLowerCase().includes(query.toLowerCase())); return <PlatformShell eyebrow="Communication" title="Messages" description="Keep buyer and supplier conversations tied to the right product or RFQ."><Card className="mt-8 overflow-hidden p-0"><div className="grid min-h-[32rem] lg:grid-cols-[19rem_1fr]"><aside className="border-b border-slate-200 lg:border-b-0 lg:border-r"><div className="p-4"><input value={query} onChange={(e) => setQuery(e.target.value)} placeholder="Search conversations" className="w-full rounded-lg border border-slate-200 px-3 py-2 text-sm" /></div><div>{filtered.map((m) => <button key={m.id} onClick={() => setSelected(m)} className={`w-full border-t border-slate-100 p-4 text-left ${selected.id === m.id ? "bg-indigo-50" : "hover:bg-slate-50"}`}><div className="flex justify-between gap-2"><strong className="text-sm">{m.name}</strong><span className="text-xs text-slate-500">{m.time}</span></div><p className="mt-1 text-xs text-indigo-700">{m.product}</p><p className="mt-1 truncate text-sm text-slate-500">{m.preview}</p></button>)}</div></aside><div className="flex flex-col"><div className="border-b border-slate-200 p-5"><div className="flex items-center gap-3"><span className={`h-2.5 w-2.5 rounded-full ${selected.online ? "bg-emerald-500" : "bg-slate-300"}`} /><div><h2 className="font-semibold">{selected.name}</h2><p className="text-sm text-slate-500">{selected.product} · {selected.online ? "Online" : "Offline"}</p></div></div></div><div className="flex-1 space-y-4 bg-slate-50 p-5"><div className="max-w-md rounded-xl rounded-tl-none bg-white p-4 text-sm text-slate-700 shadow-sm">{selected.preview}<p className="mt-2 text-xs text-slate-400">{selected.time}</p></div><div className="ml-auto max-w-md rounded-xl rounded-tr-none bg-indigo-600 p-4 text-sm text-white">Thanks. Please share the updated commercial terms for this RFQ.</div></div><div className="flex gap-2 border-t border-slate-200 p-4"><button className="rounded-lg border border-slate-200 px-3 text-slate-500" aria-label="Attach file">+</button><input placeholder="Write a message" className="min-w-0 flex-1 rounded-lg border border-slate-200 px-3 py-2 text-sm" /><button className="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Send</button></div></div></div></Card></PlatformShell>; }
