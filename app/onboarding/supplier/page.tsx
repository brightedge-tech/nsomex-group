"use client";

import { useState } from "react";
import { useRouter } from "next/navigation";
import { Container } from "@/components/ui/container";

const steps = ["Company information", "Business type", "Products and categories", "Factory information", "Certifications", "Business documents", "Company profile", "Submit for verification"];
export default function SupplierOnboardingPage() { const [step, setStep] = useState(0); const router = useRouter(); const done = step === steps.length - 1; return <main className="py-12"><Container><div className="mx-auto max-w-3xl rounded-3xl border border-slate-200 bg-white p-6 shadow-sm sm:p-8"><p className="text-xs font-semibold uppercase tracking-[0.22em] text-indigo-600">Supplier onboarding</p><div className="flex flex-wrap items-start justify-between gap-3"><div><h1 className="mt-3 text-3xl font-bold text-slate-900">Build your supplier profile</h1><p className="mt-2 text-slate-600">Verification progress keeps your application clear and reviewable.</p></div><span className="rounded-full bg-amber-50 px-3 py-2 text-xs font-semibold text-amber-700">Verification Pending</span></div><div className="mt-8 flex gap-1">{steps.map((label, index) => <div key={label} className={`h-2 flex-1 rounded-full ${index <= step ? "bg-indigo-600" : "bg-slate-100"}`} aria-label={label} />)}</div><p className="mt-3 text-xs font-semibold text-slate-500">Step {step + 1} of {steps.length}: {steps[step]}</p><div className="mt-8"><label className="text-sm font-medium text-slate-700">{steps[step]}<textarea rows={4} placeholder="Add the relevant information for this step" className="mt-2 w-full rounded-xl border border-slate-200 bg-slate-50 px-3 py-3 outline-none focus:border-indigo-500" /></label><div className="mt-8 flex justify-between gap-3"><button disabled={step === 0} onClick={() => setStep(step - 1)} className="rounded-full border border-slate-200 px-5 py-3 text-sm font-semibold text-slate-700 disabled:opacity-40">Back</button><button onClick={() => done ? router.push("/supplier/dashboard") : setStep(step + 1)} className="rounded-full bg-indigo-600 px-5 py-3 text-sm font-semibold text-white">{done ? "Submit for verification" : "Continue"}</button></div></div></div></Container></main>; }
