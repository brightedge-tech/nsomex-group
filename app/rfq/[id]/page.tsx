import { notFound } from "next/navigation";
import { rfqs, quotations } from "@/lib/data/platform";
import { PlatformShell, Status } from "@/components/platform/PlatformUI";
import { Card } from "@/components/ui/card";
import Link from "next/link";
export default async function RFQDetailPage({ params }: { params: Promise<{ id: string }> }) { const { id } = await params; const rfq = rfqs.find((item) => item.id === id); if (!rfq) return notFound(); return <PlatformShell eyebrow="RFQ details" title={rfq.title} description={`${rfq.quantity} · ${rfq.destination}`} action={<Link href="/messages" className="rounded-full bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Open messages</Link>}><div className="mt-8 grid gap-6 lg:grid-cols-[1fr_20rem]"><Card><div className="flex flex-wrap justify-between gap-3"><Status>{rfq.status}</Status><span className="text-sm text-slate-500">Posted {rfq.date}</span></div><div className="mt-6 grid gap-4 sm:grid-cols-3"><div><small className="text-slate-500">Quantity</small><p className="font-semibold">{rfq.quantity}</p></div><div><small className="text-slate-500">Destination</small><p className="font-semibold">{rfq.destination}</p></div><div><small className="text-slate-500">Deadline</small><p className="font-semibold">{rfq.deadline}</p></div></div><h2 className="mt-8 text-lg font-semibold">Supplier quotations</h2><div className="mt-4 space-y-3">{quotations.map((q) => <div key={q.supplier} className="flex flex-wrap items-center justify-between gap-3 rounded-xl border border-slate-200 p-4"><div><p className="font-semibold">{q.supplier} {q.verified && <span className="text-xs text-emerald-700">✓ Verified</span>}</p><p className="mt-1 text-sm text-slate-500">Lead time: {q.leadTime}</p></div><div className="text-right"><p className="font-semibold">{q.amount}</p><Status>{q.status}</Status></div></div>)}</div></Card><Card><h2 className="font-semibold">Request activity</h2><p className="mt-3 text-sm leading-6 text-slate-600">Supplier responses are mock data for this frontend phase. Use Messages to continue a supplier conversation.</p><Link href="/messages" className="mt-5 inline-flex text-sm font-semibold text-indigo-600">View related messages</Link></Card></div></PlatformShell>; }
